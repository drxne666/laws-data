name: Discord Notification

on:
  push:
    paths:
      - 'rules.json'  # Уведомление придет только если изменились правила

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Webhook
        run: |
          # Формируем JSON для Discord
          # Мы используем 'embeds' чтобы сообщение выглядело как красивая карточка
          curl -H "Content-Type: application/json" \
               -X POST \
               -d '{
                 "username": "Helper Updates",
                 "avatar_url": "https://github.com/github.png",
                 "embeds": [{
                   "title": "📜 База правил успешно обновлена!",
                   "description": "Парсер завершил работу. Все изменения уже доступны в приложении Las Vegas | Helper.",
                   "color": 14680411,
                   "fields": [
                     {
                       "name": "Репозиторий",
                       "value": "[drxne666/laws-data](https://github.com/drxne666/laws-data)",
                       "inline": true
                     },
                     {
                       "name": "Что изменилось?",
                       "value": "[Посмотреть правки](https://github.com/drxne666/laws-data/commits/main/rules.json)",
                       "inline": true
                     }
                   ],
                   "footer": {
                     "text": "Las Vegas Helper • Автоматическое обновление"
                   },
                   "timestamp": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"
                 }]
               }' \
               ${{ secrets.DISCORD_WEBHOOK }}
